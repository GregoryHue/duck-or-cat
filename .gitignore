# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Secrets
.env

# Scraped images (regenerated by the pipeline, not tracked)
data/raw/*
!data/raw/.gitkeep

# models/ IS tracked (model.pt via Git LFS, classes.json as a normal file)
# so the publish workflow has a model to bundle into the webapp image.

# Jupyter
.ipynb_checkpoints/

# Scrapy
scraper/.scrapy/

# Flask
instance/


scrapy.log